//Método 1 para declarar funciones:
function sumaTres(x) {
	return (x + 3)
}

//Método 2 para declarar funciones:
var sumaTres = function (x) { //almacenamos el valor de la variable sumaTres
    return x + 3;
} 

//Método 3 para declarar funciones:
var sumaTres = (x) => { //se denomina también como método de la flecha
    return x + 3;
  }
  